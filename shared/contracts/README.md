# Shared Contracts

This directory holds the shared logical vocabulary for the MVP.

- `enums/` contains canonical enum values and the first-slice allowlists
- `schemas/` contains JSON schema payload definitions
- `api/` contains TypeScript-friendly contract exports for the web app

Python code should treat the JSON files in this directory as the language-neutral source.
