# Functions

Helper SQL functions are defined in `database/migrations/002_helper_functions.sql`.

Those helpers cover:

- scenario cloning
- project summary aggregation
- pending change-set retrieval
- scenario diffing
- change-set validation
