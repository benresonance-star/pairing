# Example Data

- `sample_archicad_snapshot.json` simulates a limited Archicad read surface for the first slice
- `demo_state.seed.json` seeds the local file-backed Supabase-like runtime state
- `runtime/demo_state.json` is created automatically during local runs and is ignored by git
